# Runtime deps (so tests import the same crypto libraries as production).
-r requirements.txt

# Test framework + AWS mocking.
pytest>=8.0.0
moto[dynamodb,sqs]>=5.0.0

# Structured security testing.
bandit>=1.7.0     # static analysis (SAST) for common Python security issues
pip-audit>=2.7.0  # dependency vulnerability (CVE) scanning
